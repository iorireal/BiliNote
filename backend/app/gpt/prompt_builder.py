from app.gpt.prompt import BASE_PROMPT

note_formats = [
    {'label': '目录', 'value': 'toc'},
    {'label': '原片跳转', 'value': 'link'},
    {'label': '原片截图', 'value': 'screenshot'},
    {'label': '示意图', 'value': 'mermaid'},
    {'label': 'AI总结', 'value': 'summary'}
]

note_styles = [
    {'label': '精简', 'value': 'minimal'},
    {'label': '详细', 'value': 'detailed'},
    {'label': '学术', 'value': 'academic'},
    {"label": '教程',"value": 'tutorial', },
    {'label': '小红书', 'value': 'xiaohongshu'},
    {'label': '生活向', 'value': 'life_journal'},
    {'label': '任务导向', 'value': 'task_oriented'},
    {'label': '商业风格', 'value': 'business'},
    {'label': '会议纪要', 'value': 'meeting_minutes'}
]


# 生成 BASE_PROMPT 函数
def generate_base_prompt(title, segment_text, tags, _format=None, style=None, extras=None):
    # 生成 Base Prompt 开头部分
    prompt = BASE_PROMPT.format(
        video_title=title,
        segment_text=segment_text,
        tags=tags
    )

    # 添加用户选择的格式
    if _format:
        prompt += "\n" + "\n".join([get_format_function(f) for f in _format])

    # 根据用户选择的笔记风格添加描述
    if style:
        prompt += "\n" + get_style_format(style)

    # 添加额外内容
    if extras:
        prompt += f"\n{extras}"
    return prompt


# 获取格式函数
def get_format_function(format_type):
    format_map = {
        'toc': get_toc_format,
        'link': get_link_format,
        'screenshot': get_screenshot_format,
        'mermaid': get_mermaid_format,
        'summary': get_summary_format
    }
    return format_map.get(format_type, lambda: '')()


# 风格描述的处理
def get_style_format(style):
    style_map = {
        # ========== 精简 ==========
        'minimal': '''
## 笔记风格：精简

你必须按照以下要求输出笔记：

**结构要求：**
- 用 `##` 标题列出 3-5 个核心要点
- 每个要点下用 2-3 句话概括，不展开细节
- 总字数控制在 300-500 字

**语气要求：**
- 直接、干练，去掉所有修饰词
- 用短语而非完整长句，像”知识卡片”一样

**输出示例：**
```
## 核心观点
视频主要论证了 A 导致 B 的原因。

## 关键数据
- 实验组效率提升 40%
- 成本下降至原来的 1/3
```
''',

        # ========== 详细 ==========
        'detailed': '''
## 笔记风格：详细

你必须按照以下要求输出笔记：

**结构要求：**
- 用 `##` 标题分章节，每个章节下至少 3 段正文
- 展开每个论点，包含：核心观点、论据/例子、补充说明
- 对视频中提到的概念给出解释（即使是视频中未展开的）
- 笔记总字数 1500-3000 字

**语气要求：**
- 完整句子，逻辑连贯，像一篇深度文章
- 可以引用视频原话（用引号标注）
- 保持信息密度，不遗漏任何提到的话题
''',

        # ========== 学术 ==========
        'academic': '''
## 笔记风格：学术

你必须按照以下要求输出笔记：

**结构要求（IMRaD 格式）：**
- `## 摘要`：200 字以内概括全文
- `## 背景与问题`：研究动机、相关文献观点
- `## 方法与论证`：核心推理过程、证据链
- `## 结论与启示`：主要发现、局限性、未来方向
- `## 参考文献`：列出视频中引用的论文/著作（推断完整标题）

**语气要求：**
- 第三人称，客观中立，避免”我觉得”
- 使用学术术语，但首次出现时需解释
- 关键论断标注时间戳以便查证
''',

        # ========== 小红书 ==========
        'xiaohongshu': '''
## 笔记风格：小红书

你必须按照以下要求输出笔记：

**结构要求：**
- 开头用一句话爆款标题（加粗，带 emoji）
- 中间用短段落 + emoji 分隔，每段不超过 3 行
- 末尾加标签：`#话题1 #话题2 #话题3`

**标题技巧：**
- 正面刺激：”学会这个方法，你也能秒变高手🔥”
- 反面刺激：”再不看这个，你的工作效率就被拖垮了⚠️”

**爆款关键词词库（请自然穿插）：**
宝藏、绝绝子、手把手、建议收藏、停止摆烂、沉浸式、吐血整理、
家人们、高级感、破防了、万万没想到、狠狠拿捏、被夸爆

**语气要求：**
- 口语化、亲切、像在跟闺蜜/兄弟聊天
- 大量使用 emoji（每段至少 1 个）
- 适当夸张但不要虚假
- 用”我做过了”的口吻，不要用”你应该”的说教口吻
''',

        # ========== 教程 ==========
        'tutorial': '''
## 笔记风格：教程

你必须按照以下要求输出笔记：

**结构要求：**
- `## 前置知识`：学习本文需要的基础
- `## 核心步骤`：用编号列表，每一步一个 `###` 三级标题
- 每个步骤包含：**操作**（具体怎么做）、**原理**（为什么这样做）、**⚠️ 注意**（常见坑点）
- `## 最终结果`：完成后应该看到什么效果

**格式要求：**
- 关键命令/代码用行内代码块包裹
- 重要操作前加 ⚠️ 提醒
- 结论性语句前加 💡

**语气要求：**
- 清晰、耐心，假设读者完全零基础
- 用”首先…接着…最后…”的连接词
- 关键步骤重复强调
''',

        # ========== 生活向 ==========
        'life_journal': '''
## 笔记风格：生活向

你必须按照以下要求输出笔记：

**结构要求：**
- 用 `##` 标题按时间线或主题分段
- 每段包含：**发生了什么** + **我的感受** + **收获/思考**
- 不强求覆盖所有信息，选择最有共鸣的部分

**语气要求：**
- 第一人称”我”的视角
- 真诚、自然，像私人日记
- 可以表达不确定、疑惑、感动等情绪
- 用具体的画面感和细节，而非抽象概括

**与其它风格的核心区别：**
- 精简/详细风格关注”视频讲了什么”，生活向关注”我看了之后感受到什么”
- 允许主观评价，可以说”这个观点我不太认同”
''',

        # ========== 任务导向 ==========
        'task_oriented': '''
## 笔记风格：任务导向

你必须按照以下要求输出笔记：

**结构要求：**
- `## 目标`：1-2 句话说明最终要达成什么
- `## 行动清单`：用 `- [ ]` checklist，每项包含：任务描述 + 预计耗时 + 优先级
  - 优先级标注：🔴紧急 🟡重要 🟢可选
- `## 关键资源`：视频中提到的工具、网站、联系人
- `## 时间节点`：如果有明确截止时间，用表格列出

**格式要求：**
- 任务项以动词开头：”下载 XX”、”配置 XX”、”联系 XX”
- 每个任务可独立完成，不依赖上下文

**语气要求：**
- 指令式，像项目经理下达任务
- 去掉冗余修饰，只看”谁做什么什么时候”
''',

        # ========== 商业风格 ==========
        'business': '''
## 笔记风格：商业

你必须按照以下要求输出笔记：

**结构要求：**
- `## Executive Summary`：3-5 句话，结论先行（像给 CEO 看的摘要）
- `## 市场/行业背景`：视频讨论的商业环境
- `## 核心洞察`：用编号列表，每个洞察 = 一句话结论 + 数据支撑
- `## 可执行建议`：基于视频内容的行动建议

**格式要求：**
- 数据用**加粗**突出
- 对比用表格展示
- 每个洞察后标注 `[mm:ss]` 时间戳

**语气要求：**
- 精炼、自信，像咨询报告
- 数据驱动，能做判断的地方不做描述
- 用”推荐/不建议/有待验证”等明确表态
''',

        # ========== 会议纪要 ==========
        'meeting_minutes': '''
## 笔记风格：会议纪要

你必须按照以下要求输出笔记。视视频内容，如果视频本身并非会议/讨论，请将内容”会议化”，提炼成会议纪要格式。

**结构要求（按此顺序，不可打乱）：**
- `## 会议信息`：`- **主题：**` / `- **参与者：**`（从视频内容推断）
- `## 议题与讨论`
  - 每个议题一个 `###` 标题
  - 每个议题下：**讨论要点**（各方观点汇总）、**关键发言**（引用原话）、**争议点**（如有）
- `## 决议事项`：编号列表，每条决议一句话（必须是确定的结论）
- `## 待办事项（Action Items）`：用表格 `| 任务 | 负责人 | 截止时间 | 优先级 |`

**语气要求：**
- 客观记录，不添加个人解读
- 保留不同观点的原文表述
- 决议用”会议确定…”开头，待办用”XX 负责…”开头

**与商业风格的区别：**
- 商业风格是”给老板看的结论”，会议纪要是”给参会者看的记录”
- 会议纪要必须有待办清单和负责人
- 会议纪要保留讨论过程，商业风格只看结果
''',
    }
    return style_map.get(style, '')


# 格式化输出内容
def get_toc_format():
    return '''
    9. **目录**: 自动生成一个基于 `##` 级标题的目录。不需要插入原片跳转
    '''


def get_link_format():
    return '''
    10. **原片跳转**: 为每个主要章节添加时间戳，使用格式 `*Content-[mm:ss]`。 
    重要：**始终**在章节标题前加上 `*Content` 前缀，例如：`AI 的发展史 *Content-[01:23]`。一定是标题在前 插入标记在后
    '''


def get_screenshot_format():
    return '''
11. **原片截图**: 你收到的是一组网格图片，每个格子对应视频的一个时间点，左上角标注了 `mm:ss` 时间戳。
请严格按照以下规则为笔记插入截图标记 `*Screenshot-[mm:ss]`：

**选帧原则（按优先级）：**
1. **覆盖关键场景**: 选择最能代表该段内容的帧——展示完整画面/核心操作界面/最终结果，而非过渡/空白/模糊画面
2. **一场景一帧**: 每个关键主题选 1 张最合适的，不要对同一场景重复截图
3. **优先过程全貌**: 如果该段讲的是多步骤流程，优先选能看到最多步骤同框的画面（如代码全貌、操作界面全景），而非某个单一细节
4. **信息密度优先**: 选画面中信息量最大的帧——文字清晰、界面完整、内容可见

**数量控制:**
- 10 分钟以内视频：5-8 张截图
- 10-30 分钟视频：8-15 张截图
- 30 分钟以上：15-25 张截图

**格式（必须严格遵守）:**
- 在对应段落中插入 `*Screenshot-[mm:ss]`（如 `*Screenshot-[12:34]`）
- 时间戳必须与网格图片中的帧严格对应，不可猜测不存在的时间戳

    '''


def get_mermaid_format():
    return '''
12. **示意图**: 当内容涉及流程、架构、步骤关系、时序交互、分类对比等适合图表表达的内容时，请使用 mermaid 代码块生成示意图帮助读者理解。遵循以下规则：
- 流程图用 `flowchart`，时序交互用 `sequenceDiagram`，分类对比用 `classDiagram`
- 节点文字用中文，简洁明了
- 每张图不超过 10 个节点，避免过于复杂
- 只在确实有助于理解时使用，一般笔记中 1-3 张为宜
- 格式示例：
```mermaid
flowchart LR
    A[用户请求] --> B[API网关]
    B --> C[业务服务]
    C --> D[数据库]
```
'''


def get_summary_format():
    return '''
    13. **AI总结**: 在笔记末尾加入简短的AI生成总结,并且二级标题 就是 AI 总结 例如 ## AI 总结。
    '''
